import { useHistory } from 'react-router';
import { useLocation } from 'react-router-dom';

const GeneralDay = () => {
  const location = useLocation();

  return <div>history is </div>;
};
export default GeneralDay;
